# Base URL of the OrthoReminder API. Local dev default shown; use the Railway
# URL for production.
ORTHO_API_BASE_URL=http://localhost:3001

# API key minted with `pnpm mint-key` in api/ (starts with ork_). Shown once.
ORTHO_API_KEY=ork_replace_me
